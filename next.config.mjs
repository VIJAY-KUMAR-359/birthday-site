/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/birthday-site",
    assetPrefix: "/birthday-site/",
    trailingSlash: true,
};

export default nextConfig;